// check out some of the most important string functions
// here we will be learning regular expression
// it defines a search pattern

function addingStrings(...opt) {
  return `this is a ${opt.join(" ")}`;
}

console.log(addingStrings("hello", "What's up"));
